export const revalidate = 0;

export default function PublicMenuPage() {
  return (
    <h1 style={{ fontSize: '48px', color: 'red', textAlign: 'center', marginTop: '100px' }}>
      TEST DE DEPLOY: SI VES ESTO, EL DEPLOY FUNCIONA.
    </h1>
  );
}